export type Jam = {
  id: string;
  title: string;
  teaser: string;
  date: string;
  budget: number;
  thumbnail: string;
  thumbnailAlt: string;
}

export type Sponsor = {
  name: string;
  amount: number;
  userID: string;
};