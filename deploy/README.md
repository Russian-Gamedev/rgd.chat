# This should be easy, right?

Right now pipeline is easy:

- Analytics

  ```sh
  cd plausible
  docker-compose up -d
  ```

- Backend (Database and Storage)

  ```sh
  cd supabase/docker
  docker-compose up -d
  ```
